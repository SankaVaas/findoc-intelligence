# ── Layer 1: Ingestion + OCR + Chunking ──────────────────────────────────
# Install these first before running scripts/test_ingestion.py
#
# Run: pip install -r requirements-layer1.txt

# Core
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
loguru>=0.7.2

# File type detection
filetype>=1.2.0

# PDF text extraction
pypdf>=4.0.0

# PDF table extraction
pdfplumber>=0.10.3

# DOCX
python-docx>=1.1.0

# HTML cleaning
beautifulsoup4>=4.12.0
lxml>=5.1.0
html2text>=2020.1.16

# Language detection
langdetect>=1.0.9

# Chunking
langchain-text-splitters>=0.0.1

# ── Optional (install if you need OCR for scanned PDFs) ──────────────────
# Also requires system package: tesseract-ocr
#   Ubuntu:  sudo apt-get install tesseract-ocr tesseract-ocr-fra tesseract-ocr-deu tesseract-ocr-spa tesseract-ocr-ita
#   macOS:   brew install tesseract tesseract-lang
# pytesseract>=0.3.10
# pdf2image>=1.17.0
# Pillow>=10.2.0

# ── Optional (audio transcription) ───────────────────────────────────────
# openai-whisper>=20231117